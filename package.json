{
  "name": "health-care-surgical-mart",
  "version": "2.0.0",
  "description": "Health Care Surgical Mart - Monorepo",
  "private": true,
  "scripts": {
    "start": "cd server && npm start",
    "install-all": "cd server && npm install && cd ../client && npm install"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
